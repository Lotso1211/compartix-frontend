// Entorno de PRODUCCIÓN (npm run build).
// Cambia apiUrl por la URL real de tu backend desplegado,
// p. ej. 'https://api.compartix.com/api'. Si el frontend se sirve
// detrás del mismo dominio con un proxy inverso, '/api' es suficiente.
export const environment = {
  production: true,
  apiUrl: 'https://compartix-backend.onrender.com/api'
};
